<div class="bg-white rounded-lg p-6 md:p-10 md:border border-neutral-25 w-full md:w-auto {{ float ? 'md:absolute form-card md:shadow-card max-w-lg right-1/4 top-1/4' : '' }}">
	{% if title %}
		<h2 class="h-size h-size__groot mb-6">{{ title }}</h2>
	{% endif %}
	{{ form|raw }}
</div>
